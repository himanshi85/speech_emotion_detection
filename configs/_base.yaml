# Shared defaults for all SER models
num_classes: 8
sample_rate: 16000
data_dir: data/ravdess

dropout: 0.3
freeze_encoder: false
class_weights: true

weight_decay: 0.01
warmup_ratio: 0.1
max_grad_norm: 1.0
mixed_precision: true
early_stopping_patience: 5
seed: 42
